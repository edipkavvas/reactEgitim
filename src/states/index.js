import appState from "./app/appState";
import denemeState from "./deneme/denemeState";
import counterState from "./deneme/denemeState";

const states = {
  appState,
  denemeState,
  counterState
};

export default states;
